schema_version = "1.0.0"

id = "openrct2rg"
version = "0.1.0"
name = "OpenRCT2 Ride Generator"
tagline = "Author and export OpenRCT2 stall and facility ride objects"
maintainer = "Alex Parisi <alex@atparisi.com>"
type = "add-on"

tags = ["Import-Export", "Render"]
blender_version_min = "4.2.0"

license = ["SPDX:GPL-3.0-or-later"]

platforms = ["windows-x64", "macos-arm64", "linux-x64"]

# The bundled wheel set (regenerated by scripts/collect_wheels.py):
wheels = [
    "./wheels/openrct2_ridegenerator-0.1.0-py3-none-any.whl",
]

[permissions]
files = "Read texture images and write the generated .parkobj"
